function showForMagicians(magicians: string[]): void {
    magicians.forEach(magician => {
      console.log(magician);
    });
  }
  
  // Example usage
  const magicianss: string[] = ["Merlin", "Houdini", "David Copperfield"];
  showForMagicians(magicianss);
  